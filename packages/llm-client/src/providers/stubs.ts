/**
 * Provider stubs for @diabolicallabs/llm-client.
 *
 * All five providers (Anthropic, OpenAI, Gemini, DeepSeek, Perplexity) are now fully
 * implemented. This file is retained as the canonical location for any future provider
 * stubs added before implementation.
 *
 * If a future provider is added to the union before implementation:
 *   1. Add its createXxxProvider() stub here.
 *   2. Import from this file in client.ts.
 *   3. Remove the stub and wire the real implementation once built.
 *
 * No active stubs — all five providers are live as of Week 5.
 */

export {};
