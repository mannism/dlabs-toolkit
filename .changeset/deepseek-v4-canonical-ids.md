---
"@diabolicallabs/llm-client": patch
---

DeepSeek canonical model IDs are now `deepseek-v4-flash` and `deepseek-v4-pro` (V3/R1 IDs deprecated upstream and now alias to V4). Smoke script and README updated; no API surface change.
