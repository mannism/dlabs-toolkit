---
"@diabolicallabs/llm-client": minor
---

Pre-call hooks API (v1.5.0): `LlmHooks` with `beforeCall` and `afterCall` support on all five call types. Enables PII redaction, request mutation, short-circuit caching, and custom logging at the client level.
