---
"@diabolicallabs/llm-client": patch
---

Update `@diabolicallabs/llm-pricing` peer dependency range from `^0.4.0` to `^1.0.0`. Tracks the llm-pricing 1.0 graduation. No API changes — `computeCost` call sites are unaffected.
